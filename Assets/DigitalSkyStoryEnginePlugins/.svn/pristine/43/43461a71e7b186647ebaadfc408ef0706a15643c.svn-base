using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DavinciSync : MonoBehaviour
{
    public int TargetId = -1;

    private void Start()
    {
//         m_Client = GameObject.FindObjectOfType<DavinciGateClientHelper>();
//         if (m_Client != null)
//         {
//             m_Client.GetClient().OnConnectDelegate -= OnConnect;
//             m_Client.GetClient().OnConnectDelegate += OnConnect;
//         }
    }

    private void OnConnect()
    {
//         if(TargetId != -1)
//         {
//             m_Client.GetClient().SendCreateNodeTree(TargetId);
//         }
    }

    private void Update()
    {
//         if(m_Client == null || m_Client.GetClient().IsConnect() == false)
//         {
//             return;
//         }
// 
//         m_Client.GetClient().UpdateObject(TargetId, gameObject);
    }
}

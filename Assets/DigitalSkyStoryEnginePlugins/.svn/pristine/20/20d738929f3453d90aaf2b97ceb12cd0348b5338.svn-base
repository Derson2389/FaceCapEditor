using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DavinciCameraSync : DavinciSync
{
    private void Update()
    {
//         if (m_Client == null || m_Client.GetClient().IsConnect() == false)
//         {
//             return;
//         }
// 
//         m_Client.GetClient().UpdateCamera(TargetId, gameObject);
    }
}
